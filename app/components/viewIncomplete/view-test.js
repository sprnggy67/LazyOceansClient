'use strict';

describe('myApp.viewIncomplete module', function() {

  beforeEach(module('myApp.viewIncomplete'));

  describe('ViewIncompleteCtrl', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ViewIncompleteCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});